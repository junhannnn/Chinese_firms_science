# Raw data

Raw data are not included in this public-release folder because the source
datasets are large and come from third-party data providers. The code expects
the folder layout documented in `../docs/raw_data_inventory.md` and paths in
`../config/dataset.yml`.

Create the listed subfolders here when reproducing the pipeline locally.