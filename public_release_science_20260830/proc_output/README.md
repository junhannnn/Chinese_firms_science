# Python processed outputs

The files generated from the Python code are located in this section, but they
are not uploaded due to their large size. If you need the specific files, you
can contact the first author to request them.

Included subfolders:

- `CN_CN`
- `CNKI`
- `WOS`

See `../docs/processed_data_manifest.csv` for the file-level inventory of data
files included in the public process-data layer. The current manifest contains
no `proc_output/` data files.
